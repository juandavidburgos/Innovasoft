class AppConfig {
  
  static const bool usarBackend = true;
  static const bool modoDebug = true;

  static const String backendUrl = 'https://api.miapp.com';
  static const Duration splashDelay = Duration(seconds: 2);
}
